# ============================================================================
# LLM Lawyer — Unified Requirements File
# ============================================================================
# This file contains:
# 1. Core runtime dependencies (REQUIRED)
# 2. GPU / performance optimizations (OPTIONAL)
# 3. Development & testing tools (OPTIONAL)
#
# You do NOT need to install everything.
# Read sections carefully before installing.
# ============================================================================


# ---------------------------------------------------------------------------
# 1. CORE RUNTIME DEPENDENCIES (REQUIRED)
# ---------------------------------------------------------------------------
# These are required to:
# - run inference (CLI / API)
# - load checkpoints
# - tokenize input
# - serve the backend

torch>=2.0.0
numpy
tokenizers>=0.15.0
flask>=2.3.0
flask-cors
tqdm
regex


# ---------------------------------------------------------------------------
# 2. GPU / PERFORMANCE OPTIMIZATIONS (OPTIONAL)
# ---------------------------------------------------------------------------
# ⚠️ Install ONLY if:
# - You have an NVIDIA GPU
# - CUDA is correctly installed (11.8+)
#
# These are NOT required for inference correctness.
# They only improve speed.

# Flash Attention (very fast attention kernels)
# Install with:
#   pip install flash-attn --no-build-isolation
# flash-attn==2.5.6

# PyTorch CUDA builds
# Install from https://pytorch.org/get-started/locally/
#
# Examples (DO NOT uncomment blindly):
# torch==2.2.0+cu118
# torchvision==0.17.0+cu118
# torchaudio==2.2.0+cu118
#
# torch==2.2.0+cu121
# torchvision==0.17.0+cu121
# torchaudio==2.2.0+cu121


# ---------------------------------------------------------------------------
# 3. DEVELOPMENT / TESTING DEPENDENCIES (OPTIONAL)
# ---------------------------------------------------------------------------
# ⚠️ Install ONLY if you are:
# - developing
# - testing
# - formatting code
# - using notebooks

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Formatting & Linting
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0
mypy>=1.5.0

# Type Stubs
types-requests>=2.31.0
types-beautifulsoup4>=4.12.0

# Documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0

# Notebooks / Analysis
jupyter>=1.0.0
ipykernel>=6.25.0
matplotlib>=3.7.0
pandas>=2.0.0

# Dev Utilities
ipython>=8.12.0
rich>=13.0.0


# ============================================================================
# INSTALLATION NOTES
# ============================================================================
# 1. Basic users:
#    pip install -r requirements-all.txt
#
# 2. GPU users:
#    Install PyTorch from pytorch.org FIRST
#    Then optionally install flash-attn
#
# 3. Conda users:
#    Prefer:
#    conda install pytorch torchvision torchaudio pytorch-cuda -c pytorch -c nvidia
#
# ============================================================================
